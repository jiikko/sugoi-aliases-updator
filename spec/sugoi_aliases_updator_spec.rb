require 'spec_helper'

describe SugoiAliasesUpdator do
  it 'has a version number' do
    expect(SugoiAliasesUpdator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
